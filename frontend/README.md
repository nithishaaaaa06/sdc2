# Frontend (React) - News Reader

## Setup
1. `cd frontend`
2. `npm install`
3. Start: `npm start`
4. Edit `src/App.js` to change backend URL if needed (default http://localhost:5000)

## Notes
- Simple React app without routing for quick demo.
- JWT token stored in localStorage as `token`.
